<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
    <title>Halaman Assign Score Student</title>
  </head>
  <body>
    <div class="container">
      <h2 class="heading"><strong><%= dataStudent.fullname() %></strong></h2>
      <br>
      <form action="/subjects/assign/<%= dataStudent.id %>/<%= dataSubject.id %>/givescore" method="post">
        <div class="form group">
          <input type="text" name="score" placeholder="Input Score">
        </div>
        <br>
        <div class="form-group">
          <button type="submit" class="btn btn-primary btn-lg" style="width:30%;"><i class="fa fa-check"> Update Score</i></button>
        </div>
      </form>
    </div>
    <!-- <h1>Give Score</h1>
    <form action="/subjects/assign/<%= dataStudent.id %>/<%= dataSubject.id %>/givescore" method="post">
    <!-- <input type="text" name="StudentId" value="<%= dataStudent.id %>">
    <input type="text" name="first_name" value="<%= dataStudent.first_name %>">
    <input type="text" name="last_name" value="<%= dataStudent.last_name %>"> -->
    <!-- <input type="text" name="subject_name" value="<%= dataSubject.subject_name %>" disabled>
    <input type="text" name="score" placeholder="Input Score">
    <button type="submit" name="submit">Save</button>
    </form> -->
  </body>
</html>
